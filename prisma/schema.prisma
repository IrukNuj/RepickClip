generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite"
}

model ClipboardData {
  id        Int      @id @default(autoincrement())
  content   String
  favorite  Boolean
  count     Int
  timestamp DateTime
}
